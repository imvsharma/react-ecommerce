const CategoryItem = ({id, name}) => {
    return (
        <button class="bg-purple-900 text-white hover:bg-blue-400 font-bold py-2 px-4 mt-3 rounded">{name}</button>
    )
}

export default CategoryItem;